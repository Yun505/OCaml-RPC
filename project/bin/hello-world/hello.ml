let _ = print_endline "hello world!"
(*let _ = means that the name of var does not matter *)
(* ocamlc -o hello.byte hello.ml -> To save *)
(* ./hello.byte -> to run  *)
(* dune exec ./hello.exe -> To run and build a program in one step *)